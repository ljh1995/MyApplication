<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval"
    android:useLevel="false" >
    <solid android:color="@color/color_select_date_dialog_edit_text_bg_focus" />
    <padding
        android:left="2dp"
        android:top="1dp"
        android:right="2dp"
        android:bottom="1dp" />
    <solid
        android:color="@color/color_select_date_dialog_edit_text_bg_focus" />
    <stroke
        android:width="0dp"
        android:color="@android:color/white" />

    <!--这里宽高要一样 -->
    <size android:width="15dp"
        android:height="15dp" />
</shape>